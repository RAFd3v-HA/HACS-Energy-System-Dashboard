{
  "config": {
    "step": {
      "user": {
        "title": "Energy System Dashboard",
        "description": "Adds the technical energy and heating dashboard to the Home Assistant sidebar. Existing entities are assigned directly in the dashboard afterwards."
      }
    },
    "abort": {
      "already_configured": "Energy System Dashboard is already configured."
    }
  }
}
